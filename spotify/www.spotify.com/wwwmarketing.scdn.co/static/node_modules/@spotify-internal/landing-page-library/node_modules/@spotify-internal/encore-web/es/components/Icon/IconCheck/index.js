import React, {
    useContext
} from 'react';
import {
    EncoreContext,
    encoreContextKeyword,
    encoreContextStatus,
    getStatus
} from "../../../contexts/EncoreContext";
import {
    Experimental__IconCheck
} from "../../Experimental__Icon/icons/Experimental__IconCheck";
import {
    Icon
} from "../";
export function IconCheck(props) {
    var config = useContext(EncoreContext);
    var status = getStatus(encoreContextKeyword.icon, config);

    if (status === encoreContextStatus.next) {
        return /*#__PURE__*/ React.createElement(Experimental__IconCheck, props);
    }

    return /*#__PURE__*/ React.createElement(Icon, Object.assign({}, props, {
        dangerouslySetInnerHTML: {
            __html: "<polyline points=\"3.32 12.86 8.9 19.4 20.99 5.26\" fill=\"none\" stroke=\"#181818\"/>"
        }
    }));
}
export default IconCheck;